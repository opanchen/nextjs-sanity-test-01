const config = {
  projectId: process.env.STUDIO_PROJECT_ID as string,
  dataset: process.env.STUDIO_DATASET as string,
  apiVersion: process.env.STUDIO_API_VERSION as string,
};

export default config;
